# Evaluation Layout

- `datasets/`: labeled evaluation sets
- `reports/`: model comparison outputs
- `scripts/`: repeatable evaluation commands

Promotion should require at least:

- exact-match normalized FIO accuracy
- component-level FIO accuracy
- confidence review on low-certainty cases
- comparison against previous best model
