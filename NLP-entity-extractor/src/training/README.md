# Training Module

Contains the dataset bootstrap and training scaffold.

Implemented files:

- `labeling.py`: build reviewable JSONL records from text PDFs
- `dataset.py`: JSONL helpers
- `preprocess.py`: text normalization hook
- `train.py`: scaffold for local transformer fine-tuning
- `config/labels.json`: initial BIO-style label set
